server:
  port: 8082

spring:
  application:
    name: course-service

  profiles:
    active: dev

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    root: info
    dev.marvin.courseservice: debug


