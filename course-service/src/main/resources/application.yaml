server:
  tomcat:
    max-part-count: 1000

spring:
  application:
    name: course-service

  profiles:
    active: dev

  threads:
    virtual:
      enabled: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB
      resolve-lazily: true

logging:
  level:
    root: info
    dev.marvin.courseservice: debug


